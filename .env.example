# OT SOC-in-a-Box — Environment Configuration
# Copy this file to .env and change the passwords before deploying.
#
#   cp .env.example .env
#   nano .env
#
# WARNING: Never commit the .env file to git.

# Wazuh API credentials (Manager API at :15500)
WAZUH_API_USER=admin
WAZUH_API_PASS=CHANGE_ME_wazuh_api_password

# OpenSearch + Wazuh Dashboard credentials (:19200 and :15601)
OPENSEARCH_USER=admin
OPENSEARCH_PASS=CHANGE_ME_opensearch_password
